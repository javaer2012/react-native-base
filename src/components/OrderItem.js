import React from 'react';
import {View,Text} from 'react-native';
import {Flex,WingBlank,WhiteSpace} from 'antd-mobile-rn';

const OrderItem = props =>{
    const {orderId,src,productName,productDescription,price,}
}